#Database configuration for MySql Dev
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev
    username: sfg_dev_user
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnerDBDialect
    database: mysql
    show-sql: true

#  show-sql: true -> show the sql in the console